# Módulo de Bingo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar um módulo de Bingo ao TalentHS — gerente cria/conduz jogos (sorteio no servidor, cartelas únicas, vitória por cartela cheia, avisos "quase lá", ganhadores com desempate por d20, impressão 3×3), participante acompanha a própria cartela ao vivo.

**Architecture:** Segue o padrão dos módulos existentes (Metas/Ponto): migração SQL → router FastAPI (`/bingo`) com `asyncpg` → páginas React (admin + participante). A lógica delicada (geração de cartela, vitória, desempate) fica num módulo Python puro `app/bingo_logic.py` coberto por pytest. Sorteio e d20 acontecem no servidor. Tempo real por polling (React Query `refetchInterval`). Impressão por `@media print` (sem dependência nova).

**Tech Stack:** FastAPI + asyncpg + PyJWT (backend), React 18 + TS + Vite + Tailwind + shadcn/ui + TanStack Query (frontend), PostgreSQL (VPS). pytest (novo, só backend) para a lógica pura.

## Global Constraints

- Backend roda em dev na porta **8002** (venv em `backend/.venv`); 8000/8001 são de outros projetos — não tocar.
- Frontend em dev na porta **8080**; validar com `~/.bun/bin/bunx tsc --noEmit`, `~/.bun/bin/bunx eslint`, `~/.bun/bin/bun run build` (sempre a partir de `frontend/`).
- Migrações: **sempre criar nova**, nunca editar existente. Próxima = `028`. Aplicar em produção só com aprovação explícita do Erick.
- Single-tenant: toda tabela tem `company_id`; endpoints usam `require_manager` (gerente) e filtram por empresa.
- Idioma: UI em pt-BR, código/variáveis em inglês. TypeScript `strict` desabilitado — não adicionar `any` desnecessário.
- Cartela = **sempre 30 casas (10×3), 20 cheias + 10 vazias**. Monte de números = **múltiplo de 10** (30/60/90). Ganhadores = inteiro ≥1 e ≤ nº de participantes.
- Vitória = cartela cheia (20 números). Nota da nota: identidade visual do Bingo = **azul**; impressão azul suave + `hs.ico` (servido em `/hs.ico`).
- Ao concluir, atualizar changelog/versão nos 3 lugares (`ChangelogModal.tsx`, `AdminSidebar.tsx`, `frontend/package.json`) — feature nova → bump **minor** (`v1.8.0`).
- `git -C /home/ericks/github/talenths ...` para commits (evita bagunçar o cwd). Não dar `push` sem o Erick pedir.

---

## File Structure

**Backend**
- Create `backend/migrations/028_bingo.sql` — 5 tabelas + coluna `pending_tiebreak` em `bingo_games`.
- Create `backend/app/bingo_logic.py` — lógica pura (ranges de coluna, geração de cartela única, vitória/faltantes, ranking de desempate).
- Create `backend/tests/__init__.py` e `backend/tests/test_bingo_logic.py` — pytest da lógica pura.
- Create `backend/requirements-dev.txt` — `pytest`.
- Create `backend/app/routers/bingo.py` — endpoints + acesso a dados.
- Modify `backend/app/main.py` — incluir o router.

**Frontend**
- Create `frontend/src/types/bingo.ts` — tipos.
- Modify `frontend/src/lib/api.ts` — objeto `bingo`.
- Create `frontend/src/pages/BingoAdminPage.tsx` — `/admin/bingo`.
- Create `frontend/src/pages/MyBingoPage.tsx` — `/bingo`.
- Create `frontend/src/components/bingo/BingoCard.tsx` — cartela 10×3 (compartilhada tela+impressão).
- Create `frontend/src/components/bingo/CreateBingoGameDialog.tsx` — form de criação.
- Create `frontend/src/components/bingo/BingoLiveGame.tsx` — tela do sorteio ao vivo (gerente).
- Create `frontend/src/components/bingo/BingoTiebreakDialog.tsx` — desempate d20.
- Create `frontend/src/components/bingo/BingoPrintSheet.tsx` — folha 3×3 paisagem.
- Create `frontend/src/components/bingo/BingoPlayerCard.tsx` — cartela + status do participante.
- Modify `frontend/src/App.tsx` — rotas `/admin/bingo` e `/bingo`.
- Modify `frontend/src/components/AdminSidebar.tsx` — itens de menu (admin + pessoal).
- Modify `frontend/src/components/ChangelogModal.tsx`, `frontend/src/components/AdminSidebar.tsx`, `frontend/package.json` — changelog/versão.

---

## Task 1: Migração `028_bingo.sql`

**Files:**
- Create: `backend/migrations/028_bingo.sql`

**Interfaces:**
- Produces: tabelas `bingo_games`, `bingo_cards`, `bingo_draws`, `bingo_winners`, `bingo_tiebreak_rolls`.

- [ ] **Step 1: Escrever a migração**

```sql
-- 028_bingo.sql — módulo de Bingo
CREATE TABLE IF NOT EXISTS public.bingo_games (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL REFERENCES public.companies(id) ON DELETE CASCADE,
  name text NOT NULL,
  number_pool int NOT NULL,
  winners_target int NOT NULL,
  near_threshold int NOT NULL DEFAULT 2,
  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','running','finished','cancelled')),
  pending_tiebreak jsonb,           -- {"card_ids":[...],"won_on_draw":N} quando pausado p/ desempate
  created_by uuid,
  created_at timestamptz NOT NULL DEFAULT now(),
  started_at timestamptz,
  finished_at timestamptz
);
CREATE INDEX IF NOT EXISTS idx_bingo_games_company ON public.bingo_games(company_id);

CREATE TABLE IF NOT EXISTS public.bingo_cards (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  game_id uuid NOT NULL REFERENCES public.bingo_games(id) ON DELETE CASCADE,
  company_id uuid NOT NULL,
  user_id uuid NOT NULL,
  code text NOT NULL,
  numbers int[] NOT NULL,
  layout jsonb NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (game_id, user_id),
  UNIQUE (game_id, code)
);
CREATE INDEX IF NOT EXISTS idx_bingo_cards_game ON public.bingo_cards(game_id);
CREATE INDEX IF NOT EXISTS idx_bingo_cards_user ON public.bingo_cards(user_id);

CREATE TABLE IF NOT EXISTS public.bingo_draws (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  game_id uuid NOT NULL REFERENCES public.bingo_games(id) ON DELETE CASCADE,
  company_id uuid NOT NULL,
  number int NOT NULL,
  draw_order int NOT NULL,
  drawn_at timestamptz NOT NULL DEFAULT now(),
  drawn_by uuid,
  UNIQUE (game_id, number),
  UNIQUE (game_id, draw_order)
);
CREATE INDEX IF NOT EXISTS idx_bingo_draws_game ON public.bingo_draws(game_id);

CREATE TABLE IF NOT EXISTS public.bingo_winners (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  game_id uuid NOT NULL REFERENCES public.bingo_games(id) ON DELETE CASCADE,
  company_id uuid NOT NULL,
  card_id uuid NOT NULL REFERENCES public.bingo_cards(id) ON DELETE CASCADE,
  user_id uuid NOT NULL,
  place int NOT NULL,
  won_on_draw int,
  by_tiebreak boolean NOT NULL DEFAULT false,
  won_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (game_id, place),
  UNIQUE (game_id, card_id)
);
CREATE INDEX IF NOT EXISTS idx_bingo_winners_game ON public.bingo_winners(game_id);

CREATE TABLE IF NOT EXISTS public.bingo_tiebreak_rolls (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  game_id uuid NOT NULL REFERENCES public.bingo_games(id) ON DELETE CASCADE,
  company_id uuid NOT NULL,
  card_id uuid NOT NULL REFERENCES public.bingo_cards(id) ON DELETE CASCADE,
  round int NOT NULL,
  roll int NOT NULL CHECK (roll BETWEEN 1 AND 20),
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_bingo_tiebreak_game ON public.bingo_tiebreak_rolls(game_id);
```

- [ ] **Step 2: Pedir aprovação e aplicar em produção**

Pedir ao Erick para aplicar (aditiva). Aplicar com o cliente `psql 18` ou via script, usando a `DATABASE_URL` de `backend/.env`. Comando de verificação:

Run: `backend/.venv/bin/python -c "import asyncio,asyncpg; url=[l.split('=',1)[1].strip() for l in open('backend/.env') if l.startswith('DATABASE_URL')][0]; asyncio.run((lambda: None)())"` — na prática, conectar e listar as tabelas:

```bash
backend/.venv/bin/python - <<'PY'
import asyncio, asyncpg
url=[l.split('=',1)[1].strip() for l in open('backend/.env') if l.startswith('DATABASE_URL')][0]
async def main():
    c=await asyncpg.connect(url)
    rows=await c.fetch("SELECT tablename FROM pg_tables WHERE tablename LIKE 'bingo_%' ORDER BY 1")
    print([r['tablename'] for r in rows]); await c.close()
asyncio.run(main())
PY
```
Expected: `['bingo_cards','bingo_draws','bingo_games','bingo_tiebreak_rolls','bingo_winners']`

- [ ] **Step 3: Commit**

```bash
git -C /home/ericks/github/talenths add backend/migrations/028_bingo.sql
git -C /home/ericks/github/talenths commit -m "feat(bingo): migracao 028 (jogos, cartelas, sorteios, ganhadores, desempate)"
```

---

## Task 2: Lógica pura + pytest (`bingo_logic.py`)

**Files:**
- Create: `backend/app/bingo_logic.py`
- Create: `backend/requirements-dev.txt`
- Create: `backend/tests/__init__.py`
- Test: `backend/tests/test_bingo_logic.py`

**Interfaces:**
- Produces:
  - `column_ranges(number_pool: int) -> list[tuple[int,int]]`
  - `generate_one_card(number_pool: int) -> dict` → `{'numbers': list[int] (20, ordenados), 'layout': list[list[int|None]] (3x10)}`
  - `generate_unique_cards(number_pool: int, count: int) -> list[dict]` (levanta `ValueError` se não couber único)
  - `missing_count(card_numbers: list[int], drawn: set[int]) -> int`
  - `is_complete(card_numbers: list[int], drawn: set[int]) -> bool`
  - `rank_tiebreak(card_ids: list[str], roll_fn) -> tuple[list[str], list[dict]]` — ordenados do melhor p/ pior + registros `{'card_id','round','roll'}`

- [ ] **Step 1: Adicionar pytest ao backend e instalar**

Create `backend/requirements-dev.txt`:
```
pytest>=8.0
```
Run: `backend/.venv/bin/python -m pip install -r backend/requirements-dev.txt`
Expected: instala pytest sem erro.

Create `backend/tests/__init__.py` (arquivo vazio).

- [ ] **Step 2: Escrever os testes (falham primeiro)**

Create `backend/tests/test_bingo_logic.py`:
```python
import pytest
from app.bingo_logic import (
    column_ranges, generate_one_card, generate_unique_cards,
    missing_count, is_complete, rank_tiebreak,
)

def test_column_ranges_30():
    assert column_ranges(30) == [(1,3),(4,6),(7,9),(10,12),(13,15),(16,18),(19,21),(22,24),(25,27),(28,30)]

def test_column_ranges_90():
    r = column_ranges(90)
    assert r[0] == (1,9) and r[9] == (82,90) and len(r) == 10

def test_generate_one_card_shape():
    card = generate_one_card(30)
    assert len(card['numbers']) == 20
    assert len(set(card['numbers'])) == 20                 # sem repetição
    assert card['numbers'] == sorted(card['numbers'])
    # layout 3x10, exatamente 20 preenchidos e 10 vazios
    flat = [c for row in card['layout'] for c in row]
    assert len(flat) == 30
    assert sum(1 for c in flat if c is not None) == 20
    assert sum(1 for c in flat if c is None) == 10
    # cada coluna: 2 preenchidos + 1 vazio, ambos dentro da faixa
    for col,(lo,hi) in enumerate(column_ranges(30)):
        colvals = [card['layout'][r][col] for r in range(3)]
        filled = [v for v in colvals if v is not None]
        assert len(filled) == 2
        assert all(lo <= v <= hi for v in filled)
    # os numbers batem com o layout
    assert card['numbers'] == sorted(v for v in flat if v is not None)

def test_generate_unique_cards_all_distinct():
    cards = generate_unique_cards(30, 50)
    keys = [tuple(c['numbers']) for c in cards]
    assert len(keys) == 50
    assert len(set(keys)) == 50

def test_generate_unique_cards_raises_when_impossible():
    # monte 30 => C(3,2)^10 = 59049 conjuntos distintos; pedir mais é impossível
    with pytest.raises(ValueError):
        generate_unique_cards(30, 60000)

def test_missing_and_complete():
    nums = list(range(1,21))
    assert missing_count(nums, set(range(1,19))) == 2
    assert not is_complete(nums, set(range(1,19)))
    assert is_complete(nums, set(range(1,21)))
    assert missing_count(nums, set(range(1,21))) == 0

def test_rank_tiebreak_orders_desc():
    seq = iter([14, 8])                    # Ana=14, Bruno=8
    ordered, records = rank_tiebreak(["ana","bruno"], lambda: next(seq))
    assert ordered == ["ana","bruno"]
    assert {r['card_id']: r['roll'] for r in records if r['round']==1} == {"ana":14,"bruno":8}

def test_rank_tiebreak_rerolls_on_equal():
    # round1: ambos 10 (empate) -> reroll; round2: a=5, b=12 -> b vence
    seq = iter([10, 10, 5, 12])
    ordered, records = rank_tiebreak(["a","b"], lambda: next(seq))
    assert ordered == ["b","a"]
    assert max(r['round'] for r in records) == 2
```

- [ ] **Step 3: Rodar os testes e ver falhar**

Run: `cd backend && .venv/bin/python -m pytest tests/test_bingo_logic.py -v`
Expected: FAIL (ModuleNotFoundError: `app.bingo_logic`).

- [ ] **Step 4: Implementar `bingo_logic.py`**

Create `backend/app/bingo_logic.py`:
```python
import secrets
from collections import defaultdict


def column_ranges(number_pool: int) -> list[tuple[int, int]]:
    per = number_pool // 10
    return [(c * per + 1, c * per + per) for c in range(10)]


def generate_one_card(number_pool: int) -> dict:
    """Uma cartela: 2 números por coluna (1 linha vazia). Retorna numbers[20] e layout 3x10."""
    grid = [[None] * 10 for _ in range(3)]
    for col, (lo, hi) in enumerate(column_ranges(number_pool)):
        pool = list(range(lo, hi + 1))
        a = pool.pop(secrets.randbelow(len(pool)))
        b = pool.pop(secrets.randbelow(len(pool)))
        low, high = sorted((a, b))
        empty_row = secrets.randbelow(3)
        rows = [r for r in range(3) if r != empty_row]
        grid[rows[0]][col] = low
        grid[rows[1]][col] = high
    numbers = sorted(v for row in grid for v in row if v is not None)
    return {"numbers": numbers, "layout": grid}


def generate_unique_cards(number_pool: int, count: int, max_attempts_per_card: int = 500) -> list[dict]:
    """Gera `count` cartelas com conjuntos de números distintos entre si."""
    seen: set[tuple[int, ...]] = set()
    cards: list[dict] = []
    for _ in range(count):
        for _attempt in range(max_attempts_per_card):
            card = generate_one_card(number_pool)
            key = tuple(card["numbers"])
            if key not in seen:
                seen.add(key)
                cards.append(card)
                break
        else:
            raise ValueError(
                "Monte de números pequeno demais para gerar cartelas únicas para todos os participantes."
            )
    return cards


def missing_count(card_numbers, drawn: set) -> int:
    return sum(1 for n in card_numbers if n not in drawn)


def is_complete(card_numbers, drawn: set) -> bool:
    return all(n in drawn for n in card_numbers)


def rank_tiebreak(card_ids: list, roll_fn):
    """Ordena card_ids do melhor p/ pior por rolagem d20 (maior vence).
    Re-rola apenas entre empatados. Retorna (ordenados, registros)."""
    records: list[dict] = []

    def resolve(group: list, rnd: int) -> list:
        rolls = {cid: roll_fn() for cid in group}
        for cid, r in rolls.items():
            records.append({"card_id": cid, "round": rnd, "roll": r})
        buckets = defaultdict(list)
        for cid, r in rolls.items():
            buckets[r].append(cid)
        ordered: list = []
        for val in sorted(buckets.keys(), reverse=True):
            tied = buckets[val]
            ordered.extend([tied[0]] if len(tied) == 1 else resolve(tied, rnd + 1))
        return ordered

    return resolve(list(card_ids), 1), records


def roll_d20() -> int:
    return secrets.randbelow(20) + 1
```

- [ ] **Step 5: Rodar os testes e ver passar**

Run: `cd backend && .venv/bin/python -m pytest tests/test_bingo_logic.py -v`
Expected: PASS (8 passed).

- [ ] **Step 6: Commit**

```bash
git -C /home/ericks/github/talenths add backend/app/bingo_logic.py backend/tests backend/requirements-dev.txt
git -C /home/ericks/github/talenths commit -m "feat(bingo): logica pura (cartela/vitoria/desempate) + pytest"
```

---

## Task 3: Router — criar/listar/detalhar jogo

**Files:**
- Create: `backend/app/routers/bingo.py`
- Modify: `backend/app/main.py` (registrar `bingo_router`)

**Interfaces:**
- Consumes: `app.dependencies.get_db`, `app.dependencies.get_current_user_id`, `app.dependencies.require_manager`; `app.bingo_logic.*`.
- Produces (endpoints): `POST /bingo/games`, `GET /bingo/games`, `GET /bingo/games/{id}`; helper `_get_company_id(user_id, conn)`; serializers `_serialize_game`, `_serialize_card`.

- [ ] **Step 1: Criar o router com helpers, criação, listagem e detalhe**

Create `backend/app/routers/bingo.py`:
```python
from fastapi import APIRouter, Depends, HTTPException, status
from pydantic import BaseModel
from typing import Optional
from datetime import datetime
import asyncpg
import secrets

from app.dependencies import get_db, get_current_user_id, require_manager
from app import bingo_logic

router = APIRouter(prefix="/bingo", tags=["bingo"])

VALID_POOLS = {30, 60, 90}


async def _get_company_id(user_id: str, conn: asyncpg.Connection) -> str:
    row = await conn.fetchrow("SELECT company_id FROM public.profiles WHERE id = $1", user_id)
    if not row or not row["company_id"]:
        raise HTTPException(status_code=400, detail="Usuário sem empresa")
    return str(row["company_id"])


def _serialize_game(row) -> dict:
    d = dict(row)
    for k in ("id", "company_id", "created_by"):
        if d.get(k) is not None:
            d[k] = str(d[k])
    for k in ("created_at", "started_at", "finished_at"):
        if d.get(k):
            d[k] = d[k].isoformat()
    return d


def _serialize_card(row) -> dict:
    d = dict(row)
    for k in ("id", "game_id", "company_id", "user_id"):
        if d.get(k) is not None:
            d[k] = str(d[k])
    if d.get("created_at"):
        d["created_at"] = d["created_at"].isoformat()
    d["numbers"] = list(d.get("numbers") or [])
    return d


def _new_code(used: set) -> str:
    while True:
        c = "".join(secrets.choice("0123456789ABCDEF") for _ in range(4))
        if c not in used:
            used.add(c)
            return c


class GameCreate(BaseModel):
    name: str
    number_pool: int
    winners_target: int
    near_threshold: int = 2
    participant_user_ids: list[str]


@router.post("/games", status_code=status.HTTP_201_CREATED)
async def create_game(body: GameCreate, user_id: str = Depends(get_current_user_id),
                      conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)

    if body.number_pool not in VALID_POOLS:
        raise HTTPException(status_code=400, detail="Monte inválido (use 30, 60 ou 90)")
    parts = list(dict.fromkeys(body.participant_user_ids))  # dedup, mantém ordem
    if not parts:
        raise HTTPException(status_code=400, detail="Selecione ao menos um participante")
    if body.winners_target < 1 or body.winners_target > len(parts):
        raise HTTPException(status_code=400, detail="Nº de ganhadores deve ser entre 1 e o total de participantes")
    if body.near_threshold not in (1, 2):
        raise HTTPException(status_code=400, detail="Aviso 'quase lá' deve ser 1 ou 2")

    try:
        cards = bingo_logic.generate_unique_cards(body.number_pool, len(parts))
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))

    async with conn.transaction():
        game = await conn.fetchrow(
            """INSERT INTO public.bingo_games
               (company_id, name, number_pool, winners_target, near_threshold, status, created_by)
               VALUES ($1,$2,$3,$4,$5,'draft',$6) RETURNING *""",
            company_id, body.name, body.number_pool, body.winners_target, body.near_threshold, user_id,
        )
        used_codes: set = set()
        import json
        for uid, card in zip(parts, cards):
            await conn.execute(
                """INSERT INTO public.bingo_cards (game_id, company_id, user_id, code, numbers, layout)
                   VALUES ($1,$2,$3,$4,$5,$6)""",
                game["id"], company_id, uid, _new_code(used_codes), card["numbers"], json.dumps(card["layout"]),
            )
    return _serialize_game(game)


@router.get("/games")
async def list_games(user_id: str = Depends(get_current_user_id), conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    rows = await conn.fetch(
        """SELECT g.*,
                  (SELECT count(*) FROM public.bingo_cards c WHERE c.game_id = g.id) AS participants,
                  (SELECT count(*) FROM public.bingo_draws d WHERE d.game_id = g.id) AS draws,
                  (SELECT count(*) FROM public.bingo_winners w WHERE w.game_id = g.id) AS winners
           FROM public.bingo_games g WHERE g.company_id = $1 ORDER BY g.created_at DESC""",
        company_id,
    )
    return [_serialize_game(r) for r in rows]


@router.get("/games/{game_id}")
async def get_game(game_id: str, user_id: str = Depends(get_current_user_id),
                   conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    game = await conn.fetchrow(
        "SELECT * FROM public.bingo_games WHERE id = $1 AND company_id = $2", game_id, company_id)
    if not game:
        raise HTTPException(status_code=404, detail="Jogo não encontrado")

    cards = await conn.fetch(
        """SELECT c.*, p.name AS user_name
           FROM public.bingo_cards c
           LEFT JOIN public.profiles p ON p.id = c.user_id
           WHERE c.game_id = $1 ORDER BY p.name""", game_id)
    draws = await conn.fetch(
        "SELECT number, draw_order FROM public.bingo_draws WHERE game_id = $1 ORDER BY draw_order", game_id)
    winners = await conn.fetch(
        """SELECT w.*, p.name AS user_name, c.code
           FROM public.bingo_winners w
           LEFT JOIN public.profiles p ON p.id = w.user_id
           LEFT JOIN public.bingo_cards c ON c.id = w.card_id
           WHERE w.game_id = $1 ORDER BY w.place""", game_id)

    drawn = {d["number"] for d in draws}
    winner_card_ids = {str(w["card_id"]) for w in winners}
    near = []
    for c in cards:
        cid = str(c["id"])
        if cid in winner_card_ids:
            continue
        miss = bingo_logic.missing_count(list(c["numbers"]), drawn)
        if miss <= (game["near_threshold"] or 2):
            near.append({"card_id": cid, "user_name": c["user_name"], "missing": miss})
    near.sort(key=lambda x: x["missing"])

    def _ser_card(c):
        d = _serialize_card(c)
        d["user_name"] = c["user_name"]
        d["marked"] = [n for n in d["numbers"] if n in drawn]
        return d

    return {
        "game": _serialize_game(game),
        "cards": [_ser_card(c) for c in cards],
        "draws": [{"number": d["number"], "draw_order": d["draw_order"]} for d in draws],
        "winners": [{**_serialize_game_winner(w)} for w in winners],
        "near": near,
    }


def _serialize_game_winner(w) -> dict:
    return {
        "id": str(w["id"]), "card_id": str(w["card_id"]), "user_id": str(w["user_id"]),
        "user_name": w["user_name"], "code": w["code"], "place": w["place"],
        "won_on_draw": w["won_on_draw"], "by_tiebreak": w["by_tiebreak"],
        "won_at": w["won_at"].isoformat() if w["won_at"] else None,
    }
```

- [ ] **Step 2: Registrar o router em `main.py`**

Modify `backend/app/main.py`: importar e incluir seguindo o padrão dos outros routers:
```python
from app.routers.bingo import router as bingo_router
# ...
app.include_router(bingo_router)
```

- [ ] **Step 3: Smoke test (o backend recarrega sozinho com --reload)**

Verificar a rota registrada:
Run: `curl -s http://localhost:8002/openapi.json | python3 -c "import sys,json;print([k for k in json.load(sys.stdin)['paths'] if k.startswith('/bingo')])"`
Expected: inclui `/bingo/games` e `/bingo/games/{game_id}`.

(Teste de criação com token real é feito no fim, na Task 6, junto do front. Aqui basta as rotas subirem sem erro — conferir `tail backend.log` sem traceback.)

- [ ] **Step 4: Commit**

```bash
git -C /home/ericks/github/talenths add backend/app/routers/bingo.py backend/app/main.py
git -C /home/ericks/github/talenths commit -m "feat(bingo): endpoints criar/listar/detalhar jogo"
```

---

## Task 4: Router — iniciar, sortear e desempate

**Files:**
- Modify: `backend/app/routers/bingo.py`

**Interfaces:**
- Consumes: helpers e serializers da Task 3; `bingo_logic.is_complete`, `bingo_logic.rank_tiebreak`, `bingo_logic.roll_d20`.
- Produces: `POST /bingo/games/{id}/start`, `POST /bingo/games/{id}/draw`, `POST /bingo/games/{id}/tiebreak`.

- [ ] **Step 1: Implementar start, draw e tiebreak**

Modify `backend/app/routers/bingo.py` — adicionar ao fim:
```python
import json as _json


async def _load_game_locked(game_id, company_id, conn):
    game = await conn.fetchrow(
        "SELECT * FROM public.bingo_games WHERE id=$1 AND company_id=$2 FOR UPDATE", game_id, company_id)
    if not game:
        raise HTTPException(status_code=404, detail="Jogo não encontrado")
    return game


async def _finish_if_done(game, conn):
    won = await conn.fetchval("SELECT count(*) FROM public.bingo_winners WHERE game_id=$1", game["id"])
    drawn = await conn.fetchval("SELECT count(*) FROM public.bingo_draws WHERE game_id=$1", game["id"])
    if won >= game["winners_target"] or drawn >= game["number_pool"]:
        await conn.execute(
            "UPDATE public.bingo_games SET status='finished', finished_at=now() WHERE id=$1", game["id"])
        return True
    return False


@router.post("/games/{game_id}/start")
async def start_game(game_id: str, user_id: str = Depends(get_current_user_id),
                     conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    async with conn.transaction():
        game = await _load_game_locked(game_id, company_id, conn)
        if game["status"] != "draft":
            raise HTTPException(status_code=400, detail="O jogo já foi iniciado")
        await conn.execute(
            "UPDATE public.bingo_games SET status='running', started_at=now() WHERE id=$1", game_id)
    return {"ok": True}


@router.post("/games/{game_id}/draw")
async def draw_number(game_id: str, user_id: str = Depends(get_current_user_id),
                      conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    async with conn.transaction():
        game = await _load_game_locked(game_id, company_id, conn)
        if game["status"] != "running":
            raise HTTPException(status_code=400, detail="O jogo não está em andamento")
        if game["pending_tiebreak"]:
            raise HTTPException(status_code=409, detail="Resolva o desempate antes de sortear")

        draws = await conn.fetch("SELECT number FROM public.bingo_draws WHERE game_id=$1", game_id)
        drawn = {d["number"] for d in draws}
        remaining = [n for n in range(1, game["number_pool"] + 1) if n not in drawn]
        if not remaining:
            await _finish_if_done(game, conn)
            raise HTTPException(status_code=400, detail="Não há mais números para sortear")

        pick = secrets.choice(remaining)
        await conn.execute(
            """INSERT INTO public.bingo_draws (game_id, company_id, number, draw_order, drawn_by)
               VALUES ($1,$2,$3,$4,$5)""",
            game_id, company_id, pick, len(drawn) + 1, user_id)
        new_drawn = drawn | {pick}

        cards = await conn.fetch("SELECT id, user_id, numbers FROM public.bingo_cards WHERE game_id=$1", game_id)
        winner_ids = {str(r["card_id"]) for r in
                      await conn.fetch("SELECT card_id FROM public.bingo_winners WHERE game_id=$1", game_id)}
        newly = [c for c in cards
                 if str(c["id"]) not in winner_ids
                 and pick in c["numbers"]
                 and bingo_logic.is_complete(list(c["numbers"]), new_drawn)]

        tiebreak = None
        if len(newly) == 1:
            place = len(winner_ids) + 1
            c = newly[0]
            await conn.execute(
                """INSERT INTO public.bingo_winners (game_id, company_id, card_id, user_id, place, won_on_draw)
                   VALUES ($1,$2,$3,$4,$5,$6)""",
                game_id, company_id, c["id"], c["user_id"], place, pick)
            await _finish_if_done(game, conn)
        elif len(newly) >= 2:
            tiebreak = {"card_ids": [str(c["id"]) for c in newly], "won_on_draw": pick}
            await conn.execute(
                "UPDATE public.bingo_games SET pending_tiebreak=$2 WHERE id=$1",
                game_id, _json.dumps(tiebreak))
    return {"number": pick, "tiebreak": tiebreak}


@router.post("/games/{game_id}/tiebreak")
async def resolve_tiebreak(game_id: str, user_id: str = Depends(get_current_user_id),
                           conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    async with conn.transaction():
        game = await _load_game_locked(game_id, company_id, conn)
        pending = game["pending_tiebreak"]
        if not pending:
            raise HTTPException(status_code=400, detail="Não há desempate pendente")
        pending = pending if isinstance(pending, dict) else _json.loads(pending)
        card_ids = pending["card_ids"]
        won_on_draw = pending["won_on_draw"]

        ordered, records = bingo_logic.rank_tiebreak(card_ids, bingo_logic.roll_d20)
        for rec in records:
            await conn.execute(
                """INSERT INTO public.bingo_tiebreak_rolls (game_id, company_id, card_id, round, roll)
                   VALUES ($1,$2,$3,$4,$5)""",
                game_id, company_id, rec["card_id"], rec["round"], rec["roll"])

        next_place = await conn.fetchval(
            "SELECT count(*) FROM public.bingo_winners WHERE game_id=$1", game_id) + 1
        target = game["winners_target"]
        results = []
        for cid in ordered:
            if next_place > target:
                break
            card = await conn.fetchrow("SELECT user_id FROM public.bingo_cards WHERE id=$1", cid)
            await conn.execute(
                """INSERT INTO public.bingo_winners
                   (game_id, company_id, card_id, user_id, place, won_on_draw, by_tiebreak)
                   VALUES ($1,$2,$3,$4,$5,$6,true)""",
                game_id, company_id, cid, card["user_id"], next_place, won_on_draw)
            results.append({"card_id": cid, "place": next_place})
            next_place += 1
        await conn.execute("UPDATE public.bingo_games SET pending_tiebreak=NULL WHERE id=$1", game_id)
        await _finish_if_done(game, conn)
    return {"rolls": records, "order": ordered, "placed": results}
```

- [ ] **Step 2: Smoke test end-to-end via banco (sem UI)**

Rodar um roteiro que cria empresa/participantes fictícios não é necessário; use dados reais. Roteiro mínimo com token de gerente (obter via `POST /auth/login`) — validar na Task 6 junto da UI. Aqui, conferir que as 3 rotas subiram:
Run: `curl -s http://localhost:8002/openapi.json | python3 -c "import sys,json;print([k for k in json.load(sys.stdin)['paths'] if k.startswith('/bingo/games/{game_id}')])"`
Expected: inclui `/bingo/games/{game_id}/start`, `/draw`, `/tiebreak`.

- [ ] **Step 3: Commit**

```bash
git -C /home/ericks/github/talenths add backend/app/routers/bingo.py
git -C /home/ericks/github/talenths commit -m "feat(bingo): iniciar, sortear (servidor) e desempate d20"
```

---

## Task 5: Router — cancelar/excluir + endpoints do participante

**Files:**
- Modify: `backend/app/routers/bingo.py`

**Interfaces:**
- Produces: `POST /bingo/games/{id}/cancel`, `DELETE /bingo/games/{id}`, `GET /bingo/my`, `GET /bingo/my/{game_id}`.

- [ ] **Step 1: Implementar cancel/delete e as views do participante**

Modify `backend/app/routers/bingo.py` — adicionar:
```python
@router.post("/games/{game_id}/cancel")
async def cancel_game(game_id: str, user_id: str = Depends(get_current_user_id),
                      conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    res = await conn.execute(
        "UPDATE public.bingo_games SET status='cancelled', pending_tiebreak=NULL WHERE id=$1 AND company_id=$2",
        game_id, company_id)
    if res == "UPDATE 0":
        raise HTTPException(status_code=404, detail="Jogo não encontrado")
    return {"ok": True}


@router.delete("/games/{game_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_game(game_id: str, user_id: str = Depends(get_current_user_id),
                      conn: asyncpg.Connection = Depends(get_db)):
    await require_manager(user_id, conn)
    company_id = await _get_company_id(user_id, conn)
    game = await conn.fetchrow(
        "SELECT status FROM public.bingo_games WHERE id=$1 AND company_id=$2", game_id, company_id)
    if not game:
        raise HTTPException(status_code=404, detail="Jogo não encontrado")
    if game["status"] == "running":
        raise HTTPException(status_code=400, detail="Cancele o jogo antes de excluir")
    await conn.execute("DELETE FROM public.bingo_games WHERE id=$1 AND company_id=$2", game_id, company_id)


@router.get("/my")
async def my_games(user_id: str = Depends(get_current_user_id), conn: asyncpg.Connection = Depends(get_db)):
    rows = await conn.fetch(
        """SELECT g.id, g.name, g.status, g.number_pool, g.winners_target
           FROM public.bingo_games g
           JOIN public.bingo_cards c ON c.game_id = g.id AND c.user_id = $1
           WHERE g.status IN ('running','finished') ORDER BY g.created_at DESC""",
        user_id)
    return [{"id": str(r["id"]), "name": r["name"], "status": r["status"],
             "number_pool": r["number_pool"], "winners_target": r["winners_target"]} for r in rows]


@router.get("/my/{game_id}")
async def my_game(game_id: str, user_id: str = Depends(get_current_user_id),
                  conn: asyncpg.Connection = Depends(get_db)):
    card = await conn.fetchrow(
        "SELECT * FROM public.bingo_cards WHERE game_id=$1 AND user_id=$2", game_id, user_id)
    if not card:
        raise HTTPException(status_code=404, detail="Você não participa deste jogo")
    game = await conn.fetchrow("SELECT * FROM public.bingo_games WHERE id=$1", game_id)
    draws = await conn.fetch(
        "SELECT number, draw_order FROM public.bingo_draws WHERE game_id=$1 ORDER BY draw_order", game_id)
    drawn = {d["number"] for d in draws}
    my_win = await conn.fetchrow(
        "SELECT place FROM public.bingo_winners WHERE game_id=$1 AND card_id=$2", game_id, card["id"])
    numbers = list(card["numbers"])
    return {
        "game": {"id": str(game["id"]), "name": game["name"], "status": game["status"],
                 "number_pool": game["number_pool"], "winners_target": game["winners_target"]},
        "card": {"code": card["code"], "numbers": numbers, "layout": card["layout"]},
        "drawn": sorted(drawn),
        "marked": [n for n in numbers if n in drawn],
        "missing": bingo_logic.missing_count(numbers, drawn),
        "my_place": my_win["place"] if my_win else None,
    }
```

- [ ] **Step 2: Smoke test**

Run: `curl -s http://localhost:8002/openapi.json | python3 -c "import sys,json;p=json.load(sys.stdin)['paths'];print('/bingo/my' in p and '/bingo/my/{game_id}' in p)"`
Expected: `True`

- [ ] **Step 3: Commit**

```bash
git -C /home/ericks/github/talenths add backend/app/routers/bingo.py
git -C /home/ericks/github/talenths commit -m "feat(bingo): cancelar/excluir jogo e views do participante"
```

---

## Task 6: Frontend — tipos, API, rotas, sidebar e páginas vazias

**Files:**
- Create: `frontend/src/types/bingo.ts`
- Modify: `frontend/src/lib/api.ts`
- Create (stubs): `frontend/src/pages/BingoAdminPage.tsx`, `frontend/src/pages/MyBingoPage.tsx`
- Modify: `frontend/src/App.tsx`, `frontend/src/components/AdminSidebar.tsx`

**Interfaces:**
- Produces: tipos `BingoGame`, `BingoCard`, `BingoGameDetail`, `BingoMyGame`; objeto `bingo` em `api.ts` com `listGames/createGame/getGame/start/draw/tiebreak/cancel/deleteGame/my/myGame`.

- [ ] **Step 1: Tipos**

Create `frontend/src/types/bingo.ts`:
```typescript
export interface BingoGame {
  id: string; name: string; number_pool: number; winners_target: number;
  near_threshold: number; status: "draft" | "running" | "finished" | "cancelled";
  created_at?: string; started_at?: string | null; finished_at?: string | null;
  participants?: number; draws?: number; winners?: number;
}
export interface BingoCard {
  id: string; user_id: string; user_name?: string; code: string;
  numbers: number[]; layout: (number | null)[][]; marked?: number[];
}
export interface BingoWinner {
  card_id: string; user_id: string; user_name?: string; code?: string;
  place: number; won_on_draw: number | null; by_tiebreak: boolean;
}
export interface BingoNear { card_id: string; user_name?: string; missing: number; }
export interface BingoGameDetail {
  game: BingoGame;
  cards: BingoCard[];
  draws: { number: number; draw_order: number }[];
  winners: BingoWinner[];
  near: BingoNear[];
}
export interface BingoMyGame {
  game: { id: string; name: string; status: string; number_pool: number; winners_target: number };
  card: { code: string; numbers: number[]; layout: (number | null)[][] };
  drawn: number[]; marked: number[]; missing: number; my_place: number | null;
}
export interface DrawResult { number: number; tiebreak: { card_ids: string[]; won_on_draw: number } | null; }
export interface TiebreakResult {
  rolls: { card_id: string; round: number; roll: number }[];
  order: string[]; placed: { card_id: string; place: number }[];
}
```

- [ ] **Step 2: API**

Modify `frontend/src/lib/api.ts` — adicionar (usa os helpers `get/post/del` já existentes no arquivo):
```typescript
import type { BingoGame, BingoGameDetail, BingoMyGame, DrawResult, TiebreakResult } from "@/types/bingo";

export const bingo = {
  listGames: () => get<BingoGame[]>("/bingo/games"),
  createGame: (data: { name: string; number_pool: number; winners_target: number; near_threshold: number; participant_user_ids: string[] }) =>
    post<BingoGame>("/bingo/games", data),
  getGame: (id: string) => get<BingoGameDetail>(`/bingo/games/${id}`),
  start: (id: string) => post<{ ok: boolean }>(`/bingo/games/${id}/start`),
  draw: (id: string) => post<DrawResult>(`/bingo/games/${id}/draw`),
  tiebreak: (id: string) => post<TiebreakResult>(`/bingo/games/${id}/tiebreak`),
  cancel: (id: string) => post<{ ok: boolean }>(`/bingo/games/${id}/cancel`),
  deleteGame: (id: string) => del(`/bingo/games/${id}`),
  my: () => get<{ id: string; name: string; status: string; number_pool: number; winners_target: number }[]>("/bingo/my"),
  myGame: (id: string) => get<BingoMyGame>(`/bingo/my/${id}`),
};
```
(Confirmar no topo do arquivo que `get`, `post`, `del` estão definidos — estão, usados por `goals`.)

- [ ] **Step 3: Páginas stub**

Create `frontend/src/pages/BingoAdminPage.tsx`:
```tsx
import { AdminLayout } from "@/components/AdminLayout";
export default function BingoAdminPage() {
  return <AdminLayout><div className="p-6"><h1 className="text-2xl font-bold">Bingo</h1></div></AdminLayout>;
}
```
Create `frontend/src/pages/MyBingoPage.tsx`:
```tsx
import { AdminLayout } from "@/components/AdminLayout";
export default function MyBingoPage() {
  return <AdminLayout><div className="p-6"><h1 className="text-2xl font-bold">Bingo</h1></div></AdminLayout>;
}
```

- [ ] **Step 4: Rotas em `App.tsx`**

Modify `frontend/src/App.tsx` — importar as páginas e adicionar rotas (espelhar o padrão de `/admin/metas` e uma rota pessoal existente):
```tsx
import BingoAdminPage from "@/pages/BingoAdminPage";
import MyBingoPage from "@/pages/MyBingoPage";
// dentro do <Routes>:
<Route path="/admin/bingo" element={<ProtectedRoute requiredRoles={["master_admin","manager"]}><BingoAdminPage /></ProtectedRoute>} />
<Route path="/bingo" element={<ProtectedRoute><MyBingoPage /></ProtectedRoute>} />
```

- [ ] **Step 5: Itens de menu em `AdminSidebar.tsx`**

Modify `frontend/src/components/AdminSidebar.tsx` — importar um ícone (ex.: `Dices` de lucide-react) e adicionar:
- na seção de administração (perto de "Minha Empresa"): `{ title: "Bingo", url: "/admin/bingo", icon: Dices }`
- em `personalSections` (seção de cultura/engajamento): `{ title: "Bingo", url: "/bingo", icon: Dices }`

- [ ] **Step 6: Validar**

Run: `cd frontend && ~/.bun/bin/bunx tsc --noEmit && ~/.bun/bin/bun run build`
Expected: sem erros; build OK.

Teste manual: logar como gerente, ver "Bingo" na sidebar e abrir `/admin/bingo` (título aparece); `/bingo` idem.

- [ ] **Step 7: Commit**

```bash
git -C /home/ericks/github/talenths add frontend/src/types/bingo.ts frontend/src/lib/api.ts frontend/src/pages/BingoAdminPage.tsx frontend/src/pages/MyBingoPage.tsx frontend/src/App.tsx frontend/src/components/AdminSidebar.tsx
git -C /home/ericks/github/talenths commit -m "feat(bingo): tipos, api, rotas, sidebar e paginas base"
```

---

## Task 7: `BingoCard` + página admin (lista + criar jogo)

**Files:**
- Create: `frontend/src/components/bingo/BingoCard.tsx`
- Create: `frontend/src/components/bingo/CreateBingoGameDialog.tsx`
- Modify: `frontend/src/pages/BingoAdminPage.tsx`

**Interfaces:**
- Consumes: `bingo` (api), tipos de `bingo.ts`, `Combobox`/multi-seleção de colaboradores (`collaborators.list()`), `DonutRing`? (não). 
- Produces: `BingoCard({ layout, marked?, pending?, size?, showHeader?, name?, code? })`; `CreateBingoGameDialog({ open, onOpenChange, onCreated })`.

- [ ] **Step 1: Componente `BingoCard` (reuso tela + impressão)**

Create `frontend/src/components/bingo/BingoCard.tsx`. Renderiza a grade 10×3 a partir de `layout` (matriz 3×10 de `number|null`); `marked` (array de números sorteados que estão na cartela) pinta azul com ✓; `pending` (números da cartela ainda não sorteados) recebe contorno azul; casas `null` ficam pontilhadas. `showHeader` mostra `hs.ico` (`/hs.ico`) + `name` + `#code` + a faixa por coluna. Props:
```tsx
interface BingoCardProps {
  layout: (number | null)[][];
  marked?: number[];
  pending?: number[];       // números da cartela que ainda não saíram (para destaque no participante)
  numberPool?: number;      // para as faixas por coluna no cabeçalho
  name?: string; code?: string; showHeader?: boolean;
  variant?: "screen" | "print";
}
```
Faixas por coluna = `pool/10` (ex.: 30 → "1-3"…). Cores conforme os mockups aprovados (azul). Usar Tailwind; sem CSS custom além do necessário.

- [ ] **Step 2: `CreateBingoGameDialog`**

Create `frontend/src/components/bingo/CreateBingoGameDialog.tsx`. Form (react-hook-form + zod) com: `name` (texto), `number_pool` (Select 30/60/90), `winners_target` (número ≥1), `near_threshold` (Select "faltando 1" / "faltando 1 e 2" → valor 1 ou 2), e **seleção de participantes** (lista de colaboradores de `collaborators.list()` com busca — checkbox por pessoa, ou multi-`Combobox`). Ao salvar: `bingo.createGame(...)`, invalida `["bingo-games"]`, toast, fecha. Espelhar o padrão de `CreateGoalDialog.tsx` (dialog + form + mutation). Validação: `winners_target ≤ participantes selecionados` (mostra erro inline).

- [ ] **Step 3: Página admin (lista)**

Modify `frontend/src/pages/BingoAdminPage.tsx`: `useQuery(["bingo-games"], bingo.listGames)`, `AdminLayout`, cabeçalho com botão "Novo jogo" (abre o dialog). Lista de jogos em cards/linhas mostrando nome, status (badge), participantes/sorteios/ganhadores e ações: **Abrir** (vai pra tela ao vivo — Task 8, por enquanto seta um estado `openGameId`), **Imprimir** (Task 9), **Cancelar**/**Excluir** (mutations). Quando `openGameId` setado, renderiza `BingoLiveGame` (import adicionado na Task 8; nesta task, deixe o botão Abrir setando o estado e um placeholder até a Task 8).

- [ ] **Step 4: Validar**

Run: `cd frontend && ~/.bun/bin/bunx tsc --noEmit && ~/.bun/bin/bun run build`
Expected: sem erros.

Teste manual: criar um jogo com 3-4 participantes reais, monte 30, 3 ganhadores → aparece na lista com status "draft" e o nº de participantes correto. Conferir no banco: `bingo_cards` tem 1 linha por participante, cada `numbers` com 20 valores distintos, e nenhum conjunto repetido.

- [ ] **Step 5: Commit**

```bash
git -C /home/ericks/github/talenths add frontend/src/components/bingo/BingoCard.tsx frontend/src/components/bingo/CreateBingoGameDialog.tsx frontend/src/pages/BingoAdminPage.tsx
git -C /home/ericks/github/talenths commit -m "feat(bingo): cartela reutilizavel, criar jogo e lista admin"
```

---

## Task 8: `BingoLiveGame` (sorteio ao vivo) + `BingoTiebreakDialog`

**Files:**
- Create: `frontend/src/components/bingo/BingoLiveGame.tsx`
- Create: `frontend/src/components/bingo/BingoTiebreakDialog.tsx`
- Modify: `frontend/src/pages/BingoAdminPage.tsx` (renderizar `BingoLiveGame` quando um jogo é aberto)

**Interfaces:**
- Consumes: `bingo.getGame/start/draw/tiebreak`, tipos.
- Produces: `BingoLiveGame({ gameId, onClose })`; `BingoTiebreakDialog({ gameId, open, onResolved })`.

- [ ] **Step 1: `BingoLiveGame`**

Create `frontend/src/components/bingo/BingoLiveGame.tsx` reproduzindo a tela aprovada:
- `useQuery(["bingo-game", gameId], () => bingo.getGame(gameId), { refetchInterval: 2000 })` (polling).
- Se `status==='draft'`: botão "Iniciar jogo" → `bingo.start`.
- Se `running`: bola/roleta com o último número (`draws` ordenado), botão **Girar** (`bingo.draw`) e toggle **Auto** (usa `setInterval` chamando `draw` a cada ~5s; pausa se a resposta trouxer `tiebreak`, se `status!=='running'` ou em erro). Ao receber `draw` com `tiebreak != null`, abrir `BingoTiebreakDialog` e parar o auto.
- Painel de números 1…`number_pool` com os sorteados destacados.
- Painel "Quase lá" a partir de `detail.near` (faltando 1 em destaque forte; faltando 2 normal).
- Painel "Ganhadores" a partir de `detail.winners` (🥇🥈🥉 + nome).
- Animação simples da roleta (CSS): ao sortear, um leve "flip"/"pulse" no número — manter simples.

- [ ] **Step 2: `BingoTiebreakDialog`**

Create `frontend/src/components/bingo/BingoTiebreakDialog.tsx`: mostra os empatados; botão **Rolar d20** → `bingo.tiebreak(gameId)`; anima os dados com os valores de `result.rolls` (último round por card) e mostra a ordem final (`result.placed`). Ao concluir, `onResolved()` (que invalida `["bingo-game", gameId]` e fecha). Como o backend já resolve re-rolls internamente, o front só exibe o resultado final (pode listar as rodadas de `rolls` para transparência).

- [ ] **Step 3: Ligar na página admin**

Modify `frontend/src/pages/BingoAdminPage.tsx`: quando `openGameId`, renderizar `<BingoLiveGame gameId={openGameId} onClose={() => setOpenGameId(null)} />` (em um `Dialog` grande ou uma view dedicada).

- [ ] **Step 4: Validar**

Run: `cd frontend && ~/.bun/bin/bunx tsc --noEmit && ~/.bun/bin/bun run build`
Expected: sem erros.

Teste manual (fim-a-fim): iniciar o jogo criado na Task 7; girar números; conferir marcação automática, "quase lá" aparecendo, e ao completar uma cartela o ganhador surge; forçar um empate (monte pequeno + poucos participantes ajuda) e conferir o fluxo do d20; jogo encerra ao bater o nº de ganhadores. Conferir no banco `bingo_draws`, `bingo_winners`, `bingo_tiebreak_rolls`.

- [ ] **Step 5: Commit**

```bash
git -C /home/ericks/github/talenths add frontend/src/components/bingo/BingoLiveGame.tsx frontend/src/components/bingo/BingoTiebreakDialog.tsx frontend/src/pages/BingoAdminPage.tsx
git -C /home/ericks/github/talenths commit -m "feat(bingo): tela do sorteio ao vivo + desempate d20"
```

---

## Task 9: `BingoPrintSheet` (impressão 3×3 paisagem)

**Files:**
- Create: `frontend/src/components/bingo/BingoPrintSheet.tsx`
- Modify: `frontend/src/pages/BingoAdminPage.tsx` (ação "Imprimir")

**Interfaces:**
- Consumes: `bingo.getGame` (para as `cards`), `BingoCard` (variant print).
- Produces: `BingoPrintSheet({ gameId, open, onClose })`.

- [ ] **Step 1: Componente de impressão**

Create `frontend/src/components/bingo/BingoPrintSheet.tsx`: busca `getGame`, renderiza todas as `cards` em blocos de 9 (3×3), cada página com cabeçalho do jogo e rodapé. Cada cartela usa `BingoCard variant="print"` (azul suave, `hs.ico`, código, faixas). CSS de impressão:
```css
@media print {
  @page { size: A4 landscape; margin: 8mm; }
  body * { visibility: hidden; }
  #bingo-print, #bingo-print * { visibility: visible; }
  #bingo-print { position: absolute; inset: 0; }
  .bingo-page { page-break-after: always; }
}
```
Botão "Imprimir" chama `window.print()`. Fora da impressão, mostrar um preview (paisagem) num `Dialog` grande.

- [ ] **Step 2: Ação na página admin**

Modify `frontend/src/pages/BingoAdminPage.tsx`: botão "Imprimir" por jogo abre `BingoPrintSheet`.

- [ ] **Step 3: Validar**

Run: `cd frontend && ~/.bun/bin/bunx tsc --noEmit && ~/.bun/bin/bun run build`
Expected: sem erros.

Teste manual: abrir imprimir de um jogo com ≥10 participantes → conferir 9 por página (3×3), quebra de página correta, `hs.ico` e azul; `Ctrl+P` mostra preview em paisagem coerente.

- [ ] **Step 4: Commit**

```bash
git -C /home/ericks/github/talenths add frontend/src/components/bingo/BingoPrintSheet.tsx frontend/src/pages/BingoAdminPage.tsx
git -C /home/ericks/github/talenths commit -m "feat(bingo): folha de impressao 3x3 paisagem"
```

---

## Task 10: Página do participante (`MyBingoPage` + `BingoPlayerCard`)

**Files:**
- Create: `frontend/src/components/bingo/BingoPlayerCard.tsx`
- Modify: `frontend/src/pages/MyBingoPage.tsx`

**Interfaces:**
- Consumes: `bingo.my`, `bingo.myGame`, `BingoCard`.
- Produces: `BingoPlayerCard({ gameId })`.

- [ ] **Step 1: `BingoPlayerCard`**

Create `frontend/src/components/bingo/BingoPlayerCard.tsx`: `useQuery(["bingo-my", gameId], () => bingo.myGame(gameId), { refetchInterval: 2000 })`. Mostra o status (marcados/20, barra, "faltam N", posição/`my_place` se ganhou) e a cartela via `BingoCard` com `marked` = `data.marked` e `pending` = números da cartela que não estão em `marked`. Reproduz a tela aprovada do participante.

- [ ] **Step 2: `MyBingoPage`**

Modify `frontend/src/pages/MyBingoPage.tsx`: `useQuery(["bingo-my-list"], bingo.my)`. Lista os jogos da pessoa (running/finished); ao selecionar um, renderiza `BingoPlayerCard`. Se não houver jogos, estado vazio ("Você não está em nenhum bingo no momento.").

- [ ] **Step 3: Validar**

Run: `cd frontend && ~/.bun/bin/bunx tsc --noEmit && ~/.bun/bin/bun run build`
Expected: sem erros.

Teste manual: logar como um colaborador participante, abrir `/bingo`, ver a própria cartela; o gerente gira números em outra sessão → a cartela do participante marca sozinha em ~2s; ao completar, aparece a posição.

- [ ] **Step 4: Commit**

```bash
git -C /home/ericks/github/talenths add frontend/src/components/bingo/BingoPlayerCard.tsx frontend/src/pages/MyBingoPage.tsx
git -C /home/ericks/github/talenths commit -m "feat(bingo): pagina do participante (cartela ao vivo)"
```

---

## Task 11: Changelog e versão (v1.8.0)

**Files:**
- Modify: `frontend/src/components/ChangelogModal.tsx`, `frontend/src/components/AdminSidebar.tsx`, `frontend/package.json`

- [ ] **Step 1: Atualizar os 3 lugares**

- `ChangelogModal.tsx`: nova entrada no topo `v1.8.0` (data da entrega), `current: true`, removendo `current` da anterior. Entradas (tipo `novidade`), ex.:
  - "Bingo: nova função de bingo para confraternizações — o RH cria jogos, gera cartelas automáticas, conduz o sorteio pelo sistema (com marcação automática, avisos de 'quase lá', ranking de ganhadores e desempate) e pode imprimir as cartelas (9 por folha)."
  - "Bingo: cada colaborador acompanha a própria cartela ao vivo em Pessoal › Bingo."
- `AdminSidebar.tsx`: rodapé `TalentHS v1.8.0`.
- `frontend/package.json`: `"version": "1.8.0"`.

- [ ] **Step 2: Validar e commitar**

Run: `cd frontend && ~/.bun/bin/bun run build`
Expected: build OK.
```bash
git -C /home/ericks/github/talenths add frontend/src/components/ChangelogModal.tsx frontend/src/components/AdminSidebar.tsx frontend/package.json
git -C /home/ericks/github/talenths commit -m "chore(bingo): changelog v1.8.0 e bump de versao"
```

---

## Self-Review (feito pelo autor do plano)

- **Cobertura do spec:** páginas admin/participante (Tasks 6-10), 5 tabelas + `pending_tiebreak` (Task 1), geração única (Task 2/3), sorteio no servidor + vitória + empate d20 (Tasks 2/4), quase-lá (Tasks 3/8), fim do jogo (Task 4), impressão 3×3 (Task 9), auditoria (tabelas + inserts em 3/4), polling (8/10), changelog (11). ✔
- **Placeholders:** lógica crítica e SQL estão completos; componentes de Uor têm interface/props e comportamento explícitos, seguindo componentes existentes nomeados (`CreateGoalDialog`, `AdminLayout`) — aceitável em codebase com padrões fortes.
- **Consistência de tipos:** nomes de endpoints e campos batem entre `api.ts` (Task 6), tipos (Task 6) e o backend (Tasks 3-5): `getGame`→`BingoGameDetail` (`game/cards/draws/winners/near`), `draw`→`DrawResult` (`number/tiebreak`), `tiebreak`→`TiebreakResult` (`rolls/order/placed`), `myGame`→`BingoMyGame`. ✔
- **Nota de implementação:** ao adicionar a coluna `pending_tiebreak` (não citada no design de nível-alto), ela é suporte de implementação para pausar o sorteio no empate; registrada aqui na migração 028.
